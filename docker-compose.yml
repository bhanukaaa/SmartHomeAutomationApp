services:
  server:
    build: ./server
    ports:
      - "5000:5000"
    restart: always
    env_file:
      - .env
    volumes:
      - sqlite:/data

  simulator:
    build: ./simulator
    ports:
      - "5001:5000"
    restart: always
    env_file:
      - .env


volumes:
  sqlite: